#include "main.h"

/**
 * _strncat - Concatenates two strings using at most
 * @dest: the string that will be added
 * @src: the string that will be added on dest
 * @n: number of bytes from src that will be added to dest
 * Return: 0
 */

char *_strncat(char *dest, char *src, int n);
{

int hello, dest_len;
	
for (dest_len = 0; dest[dest_len] != '0'; dest_len++);

for (hello = 0; < n; && src[hello] != '0'; hello++)
dest[dest_len + hello] = '0';
return (dest);

}
